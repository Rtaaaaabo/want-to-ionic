import { MessageContentDatePipe } from './message-content-date.pipe';

describe('MessageContentDatePipe', () => {
  it('create an instance', () => {
    const pipe = new MessageContentDatePipe();
    expect(pipe).toBeTruthy();
  });
});
